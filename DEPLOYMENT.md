# Deployment Documentation for jsontoolsplus

This repository has been configured for automated deployment using **InitPilot**.

## 🚀 Deployment Overview

- **Application Type**: backend
- **Domain**: https://jsontoolsplus.com
- **Port**: 3101
- **Deploy Branch**: master
- **Server**: 45.61.50.181

## 🌐 Domain Setup Requirements

**IMPORTANT**: Before your application works, you must configure your domain DNS settings:

### DNS Configuration
1. **Add an A Record** for your domain:
   - **Name**: @
   - **Type**: A
   - **Value**: `45.61.50.181`
   - **TTL**: 300 (or your provider's default)

2. **If using a subdomain** (like `app.yourdomain.com`):
   - **Name**: `jsontoolsplus`
   - **Type**: A
   - **Value**: `45.61.50.181`

3. **Wait for DNS propagation** (usually 5-60 minutes)
   - Test with: `nslookup jsontoolsplus.com`
   - Should return: `45.61.50.181`

### SSL Certificate
- Caddy automatically handles SSL certificates via Let's Encrypt
- Your domain **must be publicly accessible** for SSL to work
- Certificate generation happens automatically on first request

## 📋 GitHub Secrets Required

For automated deployment to work, you need to configure the following secrets in your repository:

1. Go to **Settings** → **Secrets and variables** → **Actions**
2. Add the following repository secrets:

| Secret Name | Description | Example |
|-------------|-------------|---------|
| `SERVER_HOST` | Your server hostname or IP | `server.example.com` |
| `SERVER_USER` | SSH username for deployment | `deploy` |
| `SERVER_PASSWORD` | SSH password for server access | Your server password |

## 🔧 Deployment Process

1. **Automatic**: Push to `master` branch
2. **Manual**: Use "Run workflow" button in Actions tab

## 📁 File Changes Ignored

The following files/directories won't trigger deployments:
- `README.md`
- `pre-commit.sh`
- `LICENSE`

## 🏥 Health Checks

After deployment, the system will verify:
- ✅ Service is running
- ✅ Health endpoint: /health
- ✅ Domain accessibility
- ✅ SSL certificate

## 🛠️ Manual Deployment

To deploy manually via SSH:

```bash
ssh aj@45.61.50.181
/opt/initpilot/deployment_scripts/apps/jsontoolsplus/jsontoolsplus-deploy.sh --force
```

## 📊 Monitoring

- **Application URL**: https://jsontoolsplus.com
- **Logs**: `/var/log/jsontoolsplus`
- **Service Status**: `systemctl status jsontoolsplus-app`

## 🚨 Troubleshooting

If deployment fails:

1. Check the GitHub Actions logs
2. Verify VPS SSH connectivity
3. Check application logs on the VPS
4. Run health checks manually

---

*This deployment setup was generated by InitPilot on 2025-07-31T16:49:25.540Z*
